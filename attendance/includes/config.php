<?php
// config.php - Configuration settings

// Define the root path for the project if not already defined
if (!defined('ROOT_PATH')) {
    define('ROOT_PATH', realpath(dirname(__FILE__) . '/..')); // Go up one directory from the 'includes' folder
}
?>
